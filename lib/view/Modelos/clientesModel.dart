class Clientes{
    final String cod, nome, cpf, tel, end, cep, cidade;

    Clientes(
            {
                required this.cod,
                required this.nome,
                required this.cpf,
                required this.tel,
                required this.end,
                required this.cep,
                required this.cidade,
            }
    );
}