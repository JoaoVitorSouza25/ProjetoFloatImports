class Produto{
    final String imagem, time, preco, categoria, desc;

    Produto(
            {
                required this.imagem,
                required this.time,
                required this.preco,
                required this.categoria,
                required this.desc
            }
    );
}