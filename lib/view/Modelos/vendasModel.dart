class Vendas{
    final String pedido, data, comprador, produto, qtde, vlrtotal, pgto, cep, cidade, end;

    Vendas(
            {
                required this.pedido,
                required this.data,
                required this.comprador,
                required this.produto,
                required this.qtde,
                required this.vlrtotal,
                required this.pgto,
                required this.cep,
                required this.cidade,
                required this.end,
            }
    );
}